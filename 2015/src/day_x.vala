using advent2015;

class DayX : Day {

    uint first_u (string data) {
        var lines = data.split ("\n");
        return lines.length;
    }

    uint second_u (string data) {
        var lines = data.split ("\n");
        return lines.length;
    }
}
