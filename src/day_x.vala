using advent2015;

class DayX : Day {

    uint first (string data) {
        var lines = data.split ("\n");
        return lines.length;
    }

    uint second (string data) {
        var lines = data.split ("\n");
        return lines.length;
    }
}

